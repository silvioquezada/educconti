import { FilterUsuarioPipe } from './filter-usuario.pipe';

describe('FilterUsuarioPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterUsuarioPipe();
    expect(pipe).toBeTruthy();
  });
});
