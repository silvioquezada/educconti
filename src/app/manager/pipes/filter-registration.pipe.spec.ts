import { FilterRegistrationPipe } from './filter-registration.pipe';

describe('FilterRegistrationPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRegistrationPipe();
    expect(pipe).toBeTruthy();
  });
});
