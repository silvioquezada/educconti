import { FilterPeriodPipe } from './filter-period.pipe';

describe('FilterPeriodPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPeriodPipe();
    expect(pipe).toBeTruthy();
  });
});
